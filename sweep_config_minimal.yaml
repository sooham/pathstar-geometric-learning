# Minimal sweep configuration for quick testing
# Focuses on a few key hyperparameters
program: train.py
method: grid
metric:
  name: val/loss/overall
  goal: minimize

parameters:
  # Dataset parameters - small fixed values for quick iteration
  graph_d:
    values: [100, 1000]
  graph_l:
    value: 5
  graph_holdout_percentage:
    value: 0.2
  num_pause_tokens:
    values: [5]
  use_undirected:
    value: true
  use_directional_tokens:
    values: [true]
  use_task_tokens:
    values: [true]
  weight_tying:
    values: [true, false]
  interleave_dataset:
    value: true
  predict_direction_for_edge_task:
    values: [true, false]
  
  # Model architecture parameters - sweep these
  n_layer:
    values: [1,3]
  n_head:
    values: [8]
  n_embd:
    values: [32,64]
  dropout:
    values: [0]
  embd_dropout:
    values: [0]
  bias:
    value: true
  base:
    values: [100]
  
  # Training hyperparameters - sweep learning rate
  learning_rate:
    value: 1e-3
  label_smoothing:
    value: 0
  weight_decay:
    value: 0.01
  beta1:
    value: 0.9
  beta2:
    value: 0.95
  grad_clip:
    value: 0.0
  
  # Learning rate schedule
  decay_lr:
    value: true
  warmup_frac:
    value: 0.10
  lr_decay_frac:
    value: 0.80
  min_lr:
    value: 1e-5
  
  # Fixed training parameters
  epochs:
    value: 10_000  # Shorter for quick testing
  edge_iterations_per_epoch: # 800 edges total
    value: 10
  path_iterations_per_epoch: # 800 paths total
    value: 10
  gradient_accumulation_steps:
    value: 1  # Reduced from 4 for fewer optimizer steps, better GPU utilization
  compile:
    value: true
  eval_interval:
    value: 100  # Increased from 50 to reduce evaluation overhead
  log_interval:
    value: 100  # Increased from 10 to reduce print overhead
  print_eval_interval:
    value: 200
  vis_interval:
    value: 100
  debug_masking:
    value: true
  
  wandb_project:
    value: mats_pathstar_minimal_predict_edge_direction

